{
  "name": "lungful",
  "version": "1.0.0",
  "description": "Audio-guided breathwork app. Single HTML file, no build step.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
